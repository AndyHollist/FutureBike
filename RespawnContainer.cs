using UnityEngine;
using System.Collections;

public class RespawnContainer : MonoBehaviour {


	public GameObject[] spawnpoints;
	//respawn position can be adjusted at random

	//might have to make sure 2 people dont respawn at the same time

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
